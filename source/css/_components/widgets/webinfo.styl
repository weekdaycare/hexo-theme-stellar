.widget-wrapper.webinfo
  .widget-body
    border-radius: $border-card
    padding: 8px 16px
    background: var(--card)
    border: 1px solid var(--block-border)
    .webinfo-item
      display: flex
      padding: 8px 0
      justify-content: space-between
      align-items: center